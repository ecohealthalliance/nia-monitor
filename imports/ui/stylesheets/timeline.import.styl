.timeline
  // Shift chart over to counter phantom space
  width calc(100% + 5px)
  margin-left -5px

.timeline-range-selector-wrapper
  display flex
  align-items center
  justify-content center
  margin .5em 0 2em 0
  li
    font-size .85em
