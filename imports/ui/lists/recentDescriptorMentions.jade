template(name="recentDescriptorMentions")
  each sources
    article.centered-content
      .details
        h1
          a.promed-link.featured(href="#" uri=uri)=postSubject
        .time-link
          p {{age date}}
      table.keyword-in-context-table
        each mentionsForSource _id
          tr=kwic
  else
    +loader classes='inline'
